# Congress.gov API – Server-Side Exercise

## Setup
1. Get an API key from https://api.congress.gov/
2. Paste it into `index.js` where indicated.

## Run
```bash
npm install
npm run dev
```

## Features
- GET / : Fetches recent bills from Congress.gov
- POST /search : Filters bills by keyword + chamber
- API key auth + error handling
